{{- if and .Values.features.gatewayApi.enabled .Values.features.gatewayApi.httproutes.grafana }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-server-route
  namespace: prometheus
  annotations:
    argocd.argoproj.io/sync-wave: "22"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true,Replace=true
    link.argocd.argoproj.io/external-link: https://grafana.{{ .Values.environmentConfig.bootstrap.clusterFqdn }}
spec:
  parentRefs:
    - name: platform
      namespace: kube-system
      sectionName: https
  hostnames:
    - grafana.{{ .Values.environmentConfig.bootstrap.clusterFqdn }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: kps-grafana
          port: 80
{{- end }}
